#!/usr/bin/perl
use strict;
use warnings;
use File::Find;
use File::Basename;
use File::Copy;
use File::Spec;
use File::Path qw(make_path);
use Cwd qw(abs_path);

# TODO: will download a set of closely related genomes and run a liftover annotation from the reference genome strain
